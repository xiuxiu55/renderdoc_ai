# The Model Context Protocol Python SDK (provides FastMCP + the MCP client used
# by the web bridge). It also pulls in starlette + uvicorn, which the web bridge
# (renderdoc_mcp/web/bridge.py) uses to serve the HTTP API and the built UI.
mcp>=1.2.0
starlette>=0.37
uvicorn>=0.27

# NOTE: the 'renderdoc' module itself is NOT installable from PyPI. It is the
# native module (renderdoc.pyd / renderdoc.so) produced by building RenderDoc.
# Point RENDERDOC_MODULE_PATH at the build output directory that contains it.
